node_modules/
dist/
*.log

# Build output. Generated by `npm run build` from src/; the release workflow
# builds it rather than shipping it from the repo.
scripts/
.DS_Store
Thumbs.db
.vscode/
.idea/

# Release artifact. Built by `npm run package`; attached to a GitHub release
# rather than committed.
module.zip

# Secrets — never commit a PAT.
.env
.mcp.json

# Validation corpus is several MB and regenerable via `npm run fetch:corpus`.
test/fixtures/creature-corpus.json

# Ability DC/damage harvest, regenerable via tools/probe-ability-numbers.js.
test/fixtures/ability-numbers.json
test/fixtures/ability-numbers.tsv

# src/data/creature-tables.ts IS committed on purpose — it is generated by
# tools/fetch-creature-tables.mjs but the module must not depend on AoN at
# build time for anyone cloning the repo.

# AoN category-name harvest, regenerable via tools/fetch-category-names.mjs
test/fixtures/aon-names.json
